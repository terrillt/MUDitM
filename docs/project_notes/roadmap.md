# Roadmap

Open TODOs and planned work. Completed items move to a changelog.

---

## Upstream

- [ ] Submit upstream PRs -- 10 identified in `pull-requests.md`, PR-MUDITM-001 ready, 9 pending cherry-pick

## Testing

- [ ] SK test harness wiring -- add MUDitM `tests/` to SKMUD's `run_tests.py` glob list (same pattern as SKALD)
- [ ] First Python test -- verify normal connection produces expected log entries (Connect, IP, Shutdown complete)
